# デモ用コード

`Assets/StreamingAssets/MatsuriSamples/` に入っている。
コードエディターに貼り付けて `▶ 実行 (RUN)`（`Ctrl / ⌘ + Enter`）。

祭りは **17:00 に始まり 22:00 に終わる = 実時間2分**。
予算は **1,000,000円**。

| ファイル | ねらい | 見積 |
| --- | --- | ---: |
| `demo_1_hajimete.matsuri` | いちばん短い。屋台が1軒建つ | ¥100,000 |
| `demo_2_yatai.matsuri` | 参道の両側に屋台を並べる。値段を変えて売上を見る | ¥835,000 |
| `demo_3_ibasho.matsuri` | **盆踊り場・休憩所・神社・手水舎**で満足度を上げる | ¥983,000 |
| `demo_4_jikan.matsuri` | `時間 20:30 { 花火 "大玉" }` で見せ場を作る | ¥695,000 |
| `demo_5_jouken.matsuri` | `もし` で客の入りを見て自動増築する | ¥250,000 |
| `demo_6_honki.matsuri` | 予算を使い切る本気の構成。自動増築ぶんを残す | ¥931,000 |
| `05_natsu_no_utage.matsuri` | 屋台11種の全部入り。**FREE MODE 専用**（約¥1,690,000） | — |

同梱サンプルは全て、実際のカタログに対して**エラー0でコンパイルできること**と
**予算内に収まること**を自動テスト (`SampleCodeTests`) で保証している。

---

## いちばん短いもの

```
屋台 "たこ焼き" {
    場所 0, 6
    値段 500
}
```

## 「居場所」で満足度を上げる（今回の追加分）

屋台は買ったら終わり。長く居てもらうには居場所が要る。

```
設備 "盆踊り場" { 場所 0, -12 }   // 250,000円  踊る。遊びたい客の満足度が上がる
設備 "休憩所"   { 場所 -15, -5 }  //  60,000円  座って体力が回復する
設備 "神社"     { 場所 0, 26 }    // 200,000円  参拝する。満足度が大きく上がる
設備 "手水舎"   { 場所 -6, 21 }   //  40,000円  ついでに寄る
```

居場所は高い。屋台を減らしてでも置く価値があるか、が勝負どころになる。
実測では、盆踊り場と休憩所を置いた構成のほうが
売上が ¥509,800 → ¥546,850 に増えた（客が疲れて早く帰らなくなるため）。

## 時間で見せ場を作る

```
時間 19:00 { 太鼓 }
時間 19:40 { 盆踊り }
時間 20:30 { 花火 "大玉" }
```

## 条件で自動運転する

```
もし たこ焼き.待ち人数 > 16 {
    屋台 "型抜き" { 場所 -14, 6  値段 250 }
}

もし 売上 > 700000 {
    花火 "スペシャル"
}
```

読める値: `来場者数` / `現在の来場者` / `売上` / `予算` / `満足度` / `時刻` /
`<屋台>.待ち人数` / `<屋台>.売上` / `<屋台>.軒数`
つなぎ: `かつ` / `または`

---

## カメラ操作（トラックパッド＋キーボード）

中ボタンやホイールは使わない。**トラックパッドとキーボードだけで全部できる**。

### キーボード（これだけで完結する）

| キー | 動き |
| --- | --- |
| `W` `A` `S` `D` / 矢印 | 移動 |
| `Q` `E` | 左右に回す |
| `R` `F` | 見下ろす角度 |
| `Z` `X` | 拡大 / 縮小（`+` `-`、`PageUp` `PageDown` も可） |
| `Shift` | 加速 |
| `Space` | 会場全体を映し直す（迷子になったとき） |
| `C` | Build → Free → Visitor 切替 |

### トラックパッド

| 操作 | 動き |
| --- | --- |
| 2本指スクロール ↕ | 拡大縮小 |
| 2本指スクロール ↔ | 左右に回す |
| 1本指ドラッグ | 移動（Free / Visitor では見回し） |
| `Option` + ドラッグ | 回転（水平＋俯角） |

マウスを使う場合は、ホイールでの拡大縮小と右ドラッグでの回転も従来どおり効く。

### 文字を打っている間

**コードを書いている間はキーでカメラが動かない**（文字入力が優先される）。
会場をクリックすると入力欄から外れて、キーでも動かせるようになる。
トラックパッドの操作は文字入力中でも常に効く。
